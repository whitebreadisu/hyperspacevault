[pytest]
testpaths = app/tests
asyncio_mode = auto
markers =
    realdata: requires the real captured swuapi export fixture (BL-170 slice C); skips cleanly when SWUAPI_FIXTURE_PATH / the default export file is absent. Run with `pytest -m realdata` after fetching the fixture (see the content runbook).
