# Dev/test/lint dependencies (RR-11) — installed in CI and in the compose
# `dev` image target, never in the shipped `runtime` image.
ruff==0.16.3
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
